/** Height of the month-navigation header row (chevrons + month title) */
export const MONTHS_HEIGHT = 56;
/** Height for weekday-names header row (Mon–Sun labels) */
export const DAYS_HEADER_HEIGHT = 34;
/** Base cell size; actual layout scales via container width */
export const DATE_CELL_SIZE = 36;
