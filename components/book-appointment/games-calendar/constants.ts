/** Height of the month-navigation header row (large month name + year) */
export const MONTHS_HEIGHT = 76;
/** Height for weekday-names header row */
export const DAYS_HEADER_HEIGHT = 30;
/** Base cell size; actual layout scales via container width */
export const DATE_CELL_SIZE = 36;
