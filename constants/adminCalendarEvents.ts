/** Emitted after admin creates an appointment from the add-appointment screen so the calendar can refresh. */
export const ADMIN_CALENDAR_APPOINTMENTS_CHANGED = 'admin-calendar-appointments-changed';

/** Emitted after admin creates a recurring (fixed) appointment; settings hub refreshes and can reopen. */
export const ADMIN_RECURRING_APPOINTMENTS_CHANGED = 'admin-recurring-appointments-changed';
